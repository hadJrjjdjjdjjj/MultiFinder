import pytest
from src.solver import solve_nonpolynomial
from src.tests.utils import *

# def solve_nonpolynomial(func, initial_guesses):
#     """
#     @brief 使用 SciPy 的 fsolve 函数求解非多项式的根。
#     @param func 方程的 lambda 函数
#     @param initial_guesses 初始猜测值的列表
#     @return 非多项式方程的根
#     """


# Testing strategy:
#     func: 有无解、
#     initial_guesses:
# TODO:
def test_solve_nonpolynomial():
    pass
    # initial_guesses = generate_initial_guesses(50, -10, 10)

    # expr, symbol, coeffs, is_polynomial = parse_expression('e^x-1')
    # func = sp.lambdify(symbol, expr, 'numpy')
